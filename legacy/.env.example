# Database (Docker Compose default)
DATABASE_URL=postgresql://arsenyx:arsenyx_dev@localhost:5432/arsenyx

# GitHub OAuth (create at https://github.com/settings/developers)
GITHUB_ID=
GITHUB_SECRET=

# Better Auth (generate with: openssl rand -hex 32)
BETTER_AUTH_SECRET=

# Production only
# BETTER_AUTH_URL=https://arsenyx.com

# Screenshot API: comma-separated origins allowed without API key
# ALLOWED_SCREENSHOT_ORIGINS=https://profit-taker.com,https://www.profit-taker.com
